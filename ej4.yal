(* La sección {header} es opcional *)

(* Definiendo números hexadecimales *)

(* Definiendo las letras *)
let letter = ['a'-'z' 'A'-'Z']

(* Definiendo los números *)
let digit = ['0'-'9']
let digits = digit+

(* Definiendo los números *)
let hexdigit = (letter)(digit)